using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Pathfinding;

public class EnemyGFX : MonoBehaviour
{

    // public AIPath aiPath;

    // // Check if the enemy's pathfinding is aiming it to the left or right and flip the sprite if
    // // that's the case
    // void Update()
    // {
    //     // If the path is facing the right, flip the sprite to face that direction
    //     if (aiPath.desiredVelocity.x >= 0.01f)
    //     {
    //         transform.localScale = new Vector3(-1f, 1f, 1f);
    //     }      
    //     // If the path is facing the left, flip the sprite to face that direction
    //     else if (aiPath.desiredVelocity.x <= -0.01f)
    //     {
    //         transform.localScale = new Vector3(1f, 1f, 1f);
    //     }
    // }
}
